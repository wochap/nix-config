name: se-editors

windows:
  - viz-omni:
      root: ~/Projects/se/viz-omni
      layout: tiled

  - backend:
      root: ~/Projects/se/viz-omni/backend
      layout: tiled

  # <%- if @settings["workspaces"].split(',').include?("charts") || @settings["workspaces"].split(',').include?("currency") %>
  - frontend:
      root: ~/Projects/se/viz-omni/frontend
      layout: tiled
  # <%- end %>

  # <%- if @settings["workspaces"].split(',').include?("layout-editor") %>
  - layout-editor:
      root: ~/Projects/se/viz-omni/frontend-neo/layout-editor
      layout: tiled
  # <%- end %>

  # - organization:
  #     root: ~/Projects/se/viz-omni/frontend/organizations
  #     layout: tiled

  # <%- if @settings["workspaces"].split(',').include?("surveys") %>
  - pearson-surveys:
      root: ~/Projects/se/pearson-surveys
      layout: tiled

  # <%- if @settings["workspaces"].split(',').include?("microfrontends") %>
  - pearson-surveys-spa-parcel:
      root: ~/Projects/se/pearson-surveys/helper-scripts/spa-parcel
      layout: tiled
  # <%- end %>

  - socialexplorer-surveys:
      root: ~/Projects/se/socialexplorer-surveys
      layout: tiled
  # <%- end %>

  # <%- if @settings["workspaces"].split(',').include?("microfrontends") %>
  # - single-spa-app:
  #     root: ~/Projects/se/single-spa-demo/single-spa-app
  #     layout: tiled
  # <%- end %>

  # <%- if @settings["workspaces"].split(',').include?("microfrontends") %>
  - single-spa-root-html:
      root: ~/Projects/se/single-spa-demo/single-spa-root-html
      layout: tiled
  # <%- end %>

  # <%- if @settings["workspaces"].split(',').include?("microfrontends") || @settings["workspaces"].split(',').include?("lti") %>
  - viz-api:
      root: ~/Projects/se/viz-api
      layout: tiled
  # <%- end %>

  # - viz-websites:
  #     root: ~/Projects/se/viz-websites
  #     layout: tiled
  #     panes:
  #       - editor:
  #           - nv
